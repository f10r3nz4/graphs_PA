--run this command to populate the database: docker exec -i mysqlcontainer mysql -uroot -pVal12345-% usersdb < ./dbinit/init.sql

TODO:
--auth check
--funzione token admin check
--modellazione db per grafi check
--endpoint per la creazione di un grafo check [TODO: handleGraphCreation]
--funzione di esecuzione con aStart, aGreedy, nba con relativo salvataggio in db della simulazione 
